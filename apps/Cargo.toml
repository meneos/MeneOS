[workspace]
members = [
    "ulib",
    "init",
    "serial",
    "helloworld",
    "vmm",
    "virtio_blk",
    "fs",
]
resolver = "2"
exclude = ["syscall_compat"]

[profile.release]
panic = "abort"
opt-level = 0
strip = false
debug = true
